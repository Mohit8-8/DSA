#include<bits/stdc++.h>
using namespace std;


int main(){
    vector<int>v(10);  //ten zeroes
    vector<int>v(10,3); //ten elements of value 3
    v.pop_back(); //removes last elememnt
    v.push_back(5); //o(1)
    v.size();
    vector<int>v2=v; //copied elements of 
}