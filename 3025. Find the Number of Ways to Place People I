class Solution:
    def numberOfPairs(self, a: List[List[int]]) -> int:
        return sum(x1<=x2 and y1>=y2 and not any(x1<=x<=x2 and y2<=y<=y1
            for x,y in a if (x,y) not in ((x1,y1),(x2,y2)))
                for (x1,y1),(x2,y2) in product(a,a) if (x1,y1)!=(x2,y2))
